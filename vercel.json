{
  "buildCommand": "cd apps/web && pnpm install --ignore-scripts && pnpm build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --ignore-scripts",
  "framework": "nextjs"
}
